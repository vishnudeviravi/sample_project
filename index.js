module.exports.sum = (fNumber, sNum) => {
  return fNumber + sNum;
};

module.exports.sub = (fNum, sNum) => {
  return fNum - sNum;
};

module.exports.div = (fNum, sNum) => {
  return fNum / sNum;
};
