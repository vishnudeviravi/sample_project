const { sum, sub } = require('./index');

console.log(sum(234, 432));
console.log(sub(234, 432));

// git status - to check current status
// git add .  - to select all files to be commited
// git commit -m "message"  - to save the file to git
// git log
// git checkout commit id
// git checkout master
//  git checkout -b branchname  - to create a new branch
//  git checkout  branchname  - to switch to another branch
// git merge branchname - to merge a branch to another
